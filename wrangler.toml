# Cloudflare Pages configuration for OpenKey Web
# Cloudflare Pages looks for wrangler.toml at the repo root
name = "openkey-web"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "apps/web/.svelte-kit/cloudflare"
