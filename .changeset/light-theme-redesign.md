---
"@openkey/sdk": patch
---

Fix iframe modal sizing and update to light theme: remove 700px height cap, use scrollHeight for accurate resize reporting, match modal card background to embed content, remove dark mode overrides
