---
"@openkey/sdk": patch
---

Add `OpenKey.signOut()` for correlated OpenKey-widget session revocation and
local SDK session cleanup, enabling applications to switch OpenKey accounts
without implementing the auth protocol themselves.
