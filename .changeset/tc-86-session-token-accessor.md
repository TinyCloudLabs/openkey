---
"@openkey/sdk": minor
---

Expose `getSessionToken()` and `tinycloudSigningOptions()` so apps can construct a TinyCloud auto-sign signing strategy against `POST /api/delegate/sign` with bearer authentication.
