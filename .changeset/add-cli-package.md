---
"@openkey/cli": minor
---

Add @openkey/cli for authenticating with OpenKey from the terminal. Supports OAuth 2.1 PKCE login with a local HTTP callback server, credential storage, key management, message signing, and access token retrieval.
