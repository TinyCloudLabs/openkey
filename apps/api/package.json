{
  "name": "@openkey/api",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "echo 'No build needed - running TypeScript directly'",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openkey/db": "workspace:*",
    "@openkey/tee": "workspace:*",
    "@better-auth/oauth-provider": "^1.5.5",
    "@better-auth/passkey": "^1.5.5",
    "better-auth": "^1.5.5",
    "hono": "^4.11.3",
    "resend": "^6.6.0",
    "viem": "^2.43.5",
    "@tinycloud/node-sdk": "^2.0.0",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
